<!-- res/drawable/spinner_border.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent"/> <!-- Fondo transparente -->
    <stroke
        android:width="2dp"
        android:color="@color/bordo"/> <!-- Color y grosor del borde -->
    <corners android:radius="5dp"/> <!-- Bordes redondeados -->
</shape>